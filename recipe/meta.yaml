{% set name = "girder" %}
{% set version = "3.2.13" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 0728f7ea3f94900208d3f6d68722f19241ce434ef3cad6e295dec7470e18a985

build:
  entry_points:
    - girder-server = girder.cli.serve:main
    - girder = girder.cli:main
    - girder-sftpd = girder.cli.sftpd:main
    - girder-shell = girder.cli.shell:main
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools_scm
    - setuptools
  run:
    - python >={{ python_min }}
    - boto3
    - botocore
    - cherrypy
    - cheroot <11
    - click
    - click-plugins
    - dogpile.cache
    - filelock
    - jsonschema
    - mako
    - packaging
    - paramiko
    - passlib
    - pymongo >=4
    - pyyaml
    - psutil
    - pyopenssl
    - python-dateutil
    - pytz
    - requests
    - pyotp

test:
  imports:
    - girder
    - girder.api
    - girder.api.v1
    - girder.models
    - girder.utility

  commands:
    - girder-server --help
    - girder --help
    - girder-sftpd --help
    - girder-shell --help
    - pip check

  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/girder/girder
  license: Apache-2.0
  license_file: LICENSE
  license_family: Apache
  summary: Web-based data management platform
  dev_url: https://github.com/girder/girder
  doc_url: https://girder.readthedocs.org

extra:
  recipe-maintainers:
    - pmlandwehr
    - jan-janssen
